export function translateFrenchToRussian(wordToTranslate: string): string {
  // curl -o response.txt "http://translate.google.ru/translate_a/t?client=x&text=salut&hl=fr&sl=fr&tl=ru"
  // translatedText = myJSON.sentences[0].trans
  console.log(wordToTranslate);
  return "м волки";
}
