// sentenceSplitter
// ✕ Should return a splitted version of the argument string (5 ms)
// sentenceJoiner
// ✕ Should return a single string, joining the elements of the input array
// Using both functions
// ✕ There should be no change when passing a sentence in sentenceSplitter then sentenceJoiner (1 ms)

export const sentenceJoiner = (stringArray: string[]): string => {
  // Code the sentenceJoiner function here
  return stringArray.join(" ");
};
